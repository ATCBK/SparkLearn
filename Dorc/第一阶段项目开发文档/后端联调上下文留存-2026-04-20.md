# SparkLearn 后端联调上下文留存（2026-04-20）

## 1. 本文档目的
- 固化当前对话中已确认的后端联调范围、协议与待补信息。
- 作为后续后端开发、联调、测试的执行基线。

## 2. 已确认决策（用户拍板）
1. 本期就做（不是只做规划），包含核心后端联调能力。
2. 流式协议统一使用 SSE envelope 方案：
   - `data: {"type":"...","payload":{...}}`
3. 固定单用户方案：
   - `single_user`
4. 质量目标：
   - 后端单测 + API 测试全过
   - 前端 Playwright 在真实 API 联调模式下再跑一轮
5. 当前阶段要求：
   - 先整理清楚所需 API 与参考文档，暂不立即编码

## 3. 当前仓库状态（已核查）
- 前端存在并可运行：`frontend/`
- 文档存在并作为主参考：
  - `Dorc/后端设计文档.md`
  - `Dorc/后端技术文档（赛题对齐版）.md`
- 前端 API 目前是 Mock 实现：
  - `frontend/src/lib/api/index.ts`
  - `frontend/src/lib/api/mock.ts`
  - `frontend/src/lib/api/types.ts`
- 当前仓库尚无落地的后端代码目录（无可直接运行的 `backend/` 实现）。

## 4. 对接外部 API（待用户提供）
以下外部能力需要文档与密钥：

1. 讯飞星火 Chat API
   - 用途：`/api/tutor/chat`、`/api/profile/chat`、`/api/resources/generate`
2. 讯飞 Embedding API
   - 用途：知识库向量化与检索
3. 讯飞 ASR API（可选）
   - 用途：`/api/voice/asr`
4. 讯飞 TTS API（可选）
   - 用途：`/api/voice/tts`
5. 如需真实 `ppt/video` 产出，还需对应第三方生成 API

每个外部 API 需要提供：
- 官方文档链接
- 鉴权方式
- 请求/响应示例
- 限流与错误码说明
- 可用环境变量键名与密钥注入方式

## 5. 项目内后端接口范围（目标清单）
按文档对齐，拟实现/联调接口：

1. `POST /api/profile/onboarding`
2. `POST /api/profile/initiate`
3. `POST /api/profile/chat`（SSE envelope）
4. `GET /api/profile`
5. `PUT /api/profile`
6. `GET /api/learning-path`
7. `POST /api/learning-path/adjust`
8. `GET /api/tasks/today`
9. `PUT /api/tasks/{task_id}/complete`
10. `GET /api/resources`
11. `POST /api/resources/generate`（SSE envelope）
12. `GET /api/resources/{resource_id}`
13. `GET /api/quiz`
14. `POST /api/quiz/submit`
15. `GET /api/quiz/wrong`
16. `POST /api/tutor/chat`（SSE envelope）
17. `GET /api/tutor/history`
18. `GET /api/evaluation/report`
19. `POST /api/evaluation/refresh`
20. `GET /api/contribution`
21. `POST /api/voice/asr`（可选）
22. `POST /api/voice/tts`（可选）

## 6. 前端已使用但后端文档未完全对齐的能力（待定策略）
前端 mock 里有，但后端主文档未给明确 endpoint 的能力：

1. `getDashboardStats`
2. `getRecentResources`
3. `getMasteryData`
4. `getRecommendations`
5. `getVideos`
6. `getDailyQuote`

待定两种方案：
- 方案 A：新增后端独立接口
- 方案 B：前端 `real.ts` 适配聚合已有接口

## 7. 文档间关键对齐点
1. 已确定以 SSE envelope 为统一流式输出标准（覆盖 `profile/chat`、`tutor/chat`、`resources/generate`）。
2. onboarding 同时支持：
   - 问卷入口：`/api/profile/onboarding`
   - 对话入口：`/api/profile/initiate` + `/api/profile/chat`
3. 固定用户 `single_user` 作为第一阶段默认联调身份。

## 8. 下一步（收到 SDK 后）
1. 接入并验证星火 SDK（含最小可运行 demo）。
2. 确认 SDK 与 SSE envelope 的桥接方式（流式 chunk -> 统一事件）。
3. 建立后端骨架并先打通：
   - `quiz`（服务端判题）
   - `tutor`（SSE）
   - `generate`（SSE）
   - `onboarding`（问卷 + 对话）
4. 前端从 `mock` 切到 `real.ts`，进行端到端联调。
5. 执行后端测试与 Playwright 真实 API 回归。

## 9. 你可以这样给 SDK
- 方式 1：给本地路径（例如压缩包解压目录）
- 方式 2：给 Git 仓库地址
- 方式 3：给 pip 包名 + 版本 + 官方文档

